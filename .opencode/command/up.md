---
description: Start the Tempo tracing backend (compose up) and confirm readiness.
agent: build
---

# Up

Run `make up` from the repo root and confirm the backend is ready
(`curl http://127.0.0.1:3200/ready` must print `ready`).
