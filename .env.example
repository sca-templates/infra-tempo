# Non-secret defaults — copy to .env or let `make setup` create it.
# Tempo holds no credentials locally (stateless telemetry store); ports are
# the standard ones declared in tempo/tempo.yml.

ENVIRONMENT=dev

TEMPO_HTTP_PORT=3200
TEMPO_OTLP_GRPC_PORT=4317
TEMPO_OTLP_HTTP_PORT=4318
